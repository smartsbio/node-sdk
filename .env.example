# smarts.bio API key — generate one at chat.smarts.bio → Organization Settings → API Keys
SMARTSBIO_API_KEY=sk_live_...

# Workspace ID to run tests against — find it in your workspace settings
SMARTSBIO_WORKSPACE_ID=your-workspace-id

# Optional: run pipeline create/cancel tests (uses queue capacity)
# RUN_PIPELINE_TESTS=true
